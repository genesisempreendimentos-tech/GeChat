{
  "name": "geleads",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"wait-on file:backend/.server-port && npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:prod": "npm run build --prefix frontend && node -e \"require('fs').rmSync('backend/dist',{recursive:true,force:true}); require('fs').cpSync('frontend/dist','backend/dist',{recursive:true})\"",
    "postinstall": "npm run install:all"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.3"
  }
}
