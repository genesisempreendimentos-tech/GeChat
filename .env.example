# UI shell mock — variáveis de backend não são necessárias para `npm run dev`.
# Deixe vazio ou preencha só se for experimentar cliente Supabase localmente.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
