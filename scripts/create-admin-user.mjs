#!/usr/bin/env node
/**
 * Este repositório é apenas mock de UI — não cria usuários em backend real.
 * Login: use os e-mails em `src/mocks/uiShellData.ts` (lista fixa no authStore).
 */
console.log(
  '[ui-shell] create-admin-user desativado: projeto só com mock de design. Veja src/mocks/uiShellData.ts.',
);
process.exit(0);
